function Wrapped(props) {
    const token = props.token;

    return (
        <div className="App">
            <header className="App-header">
                <p>Wrapped!</p>
            </header>
        </div>
    );
}

export default Wrapped;